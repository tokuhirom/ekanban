{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ボードのウィンドウが webview から使えるもの。使うものだけを並べる（docs/TAURI-MIGRATION.md §9）。",
  "windows": ["board", "capture"],
  "permissions": ["core:event:default"]
}
