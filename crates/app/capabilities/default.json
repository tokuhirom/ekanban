{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ボードのウィンドウが webview から使えるもの。使うものだけを並べる（docs/DESIGN.md「画面の作り」）。",
  "windows": ["board", "capture"],
  "permissions": ["core:event:default"]
}
